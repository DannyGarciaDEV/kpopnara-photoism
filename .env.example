# Required in all environments
MONGODB_URI=mongodb://localhost:27017
# Production (e.g. Atlas): mongodb+srv://user:pass@cluster.mongodb.net/dbname

# JWT for staff auth (min 32 characters)
JWT_SECRET=your-secret-at-least-32-characters-long

# Production only: required to run POST /api/seed (set a long random string)
# SEED_SECRET=optional-long-random-string-for-seed-endpoint
